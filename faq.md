#### Run node on LINUX AWS
/lib64/libc.so.6: version `GLIBC_2.14' not found. Why am I getting this error?
brew install glibc

#### brew install vim and link vim already but version don't change?
restart terminal or logout and login again
